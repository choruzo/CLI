{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos de la ventana principal. D0 solo necesita el núcleo de Tauri: el sidecar se lanza desde Rust con std::process (sin plugin shell) y todo el acceso a ficheros de proyecto se enrutará por el sidecar (15.8).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
