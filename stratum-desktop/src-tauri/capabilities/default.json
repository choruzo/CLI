{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos de la ventana principal. El sidecar se lanza desde Rust con std::process (sin plugin shell) y todo acceso a ficheros pasa por el sidecar (15.8). D1 añade solo abrir enlaces de las respuestas en el navegador del sistema, limitado a http(s) y mailto: el webview nunca navega fuera de la app.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://*" }, { "url": "http://*" }, { "url": "mailto:*" }]
    }
  ]
}
